# Secrets / local config
.env
*.env
!.env.example

# Agent outbound queue (drafts awaiting human approval) — local only
outbox/
*.outbox.json

# Actual test-run artefacts are committed under results/runs/, but local
# scratch runs go here:
results/scratch/*
# ИСКЛЮЧЕНИЕ: артефакт реального прогона Ouroboros (task dec66d75) — публикуется
# как proof ГигаАгента (презентация сл.1/5/6, README ссылаются на result.json/run.log).
!results/scratch/ouroboros_demo/
*.local.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / npm cache (not part of the project)
npm-cache/
node_modules/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
