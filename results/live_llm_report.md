# Live LLM прогон тестовой корзины (GLM 5.2)

- **Run ID**: `eval_glm52_20260720`
- **Движок**: `llm` (реальная LLM, не mock)
- **Модель**: `z-ai/glm-5.2` через OpenRouter
- **Навык**: v1 (stable)
- **Дата**: 2026-07-20
- **Стоимость**: ~$0.024 (15 LLM-вызовов)
- **Время**: 10.898 с среднее, p50=7.12 с, p95=43.57 с

## Метрики (micro по корзине, строгое текстовое сопоставление)

| Метрика | GLM 5.2 | gpt-4o-mini (предыдущий) | Rule-engine (канон) | Цель |
|---|---|---|---|---|
| Поручения precision | 18.2% | 10.0% | 100.0% | ≥85% |
| Поручения recall | 18.2% | 9.1% | 100.0% | ≥80% |
| Поручения F1 | 18.2% | 9.5% | 100.0% | ≥82% |
| Решения F1 | 84.6% | 53.8% | 100.0% | — |
| Блокеры F1 | 100.0% | 100.0% | 100.0% | — |
| Сводка F1 | 94.1% | 97.7% | 100.0% | — |
| Доля успешных прогонов | 41.2% (7/17) | 23.5% (4/17) | 100.0% (17/17) | — |
| Evidence coverage | 100.0% | 100.0% | 100.0% | — |
| Принятые черновики | 100.0% | 100.0% | 100.0% | ≥60% |
| Среднее время | 10.898 с | 2.016 с | 0.004 с | ≤180 с |

## GLM 5.2 vs gpt-4o-mini

GLM 5.2 показывает **значительно лучшие результаты**:
- Поручения F1: 18.2% vs 9.5% (+92% относительно)
- Решения F1: 84.6% vs 53.8% (+57% относительно)
- Успешных прогонов: 41.2% vs 23.5% (+75% относительно)

Однако обе модели далеки от rule-engine (100%) на синтетической корзине из-за строгого текстового сопоставления.

## Почему метрики LLM ниже rule-engine

1. **Строгое текстовое сопоставление.** Харнесс сравнивает поля `action/owner/due/source` по точной нормализованной строке. LLM парафразирует: «подготовить release-notes» vs ожидаемое «подготовлю release-notes» — семантически верно, но не засчитывается.

2. **Пустые actions.** GLM 5.2 иногда возвращает `actions:[]` для коротких реплик, где rule-engine находит поручение по паттерну.

3. **Source не совпадает.** LLM ставит `source: "расшифровка синка 03.07"`, эталон — `source: "APP-412"` (ID задачи). Семантически оба верны, но matching по 4 полям требует точного совпадения.

## Что доказывает live LLM прогон

- **LLM-интеграция работает end-to-end**: реальные API-вызовы, JSON-валидация, ретраи, cost tracking, fallback.
- **Safety Layer работает**: недоверенные расшифровки изолируются, prompt injection блокируется (TB-11 success).
- **Память и HITL работают**: TB-14 (недоступный трекер) success, TB-15 (повреждённая расшифровка) success.
- **Бюджет контролируется**: ~$0.024 на 17 сценариев.

## Канонические метрики

Канонические метрики MVP (rule-engine, `eval_20260719T_fixed`) остаются первичными:
P=100%, R=100%, F1=100% — доказывают работу конвейера, Safety Layer, памяти, HITL и версионирования.

Live LLM прогон — дополнительное измерение, показывающее что LLM-путь работает с GLM 5.2, но требует семантического матчинга для метрик, сопоставимых с rule-engine.

## Артефакты

- `results/runs/eval_glm52_20260720/` — полные результаты прогона
- `results/runs/eval_glm52_20260720/llm_cost.log` — лог стоимости 15 вызовов
- `results/runs/eval_glm52_20260720/metrics.json` — агрегированные метрики
- `results/runs/eval_glm52_20260720/results_summary.md` — сводка
- `results/runs/eval_glm52_20260720/evaluation_report.html` — HTML-отчёт

## Баг, обнаруженный и исправленный при live LLM прогоне

`_parse_json_reply` в `src/athanor/llm.py` использовал `rfind("}")` для поиска конца JSON. LLM иногда добавляет лишнюю `}` (e.g., `{...}}`), что давало `JSONDecodeError: Extra data`. Исправлено через `json.JSONDecoder().raw_decode()` — парсит первый валидный объект, игнорируя хвост. 126 тестов проходят.
