# Ouroboros E2E Demo — 2026-07-20

Дополнительное демо-видео: **живой сквозной прогон агента Ouroboros** через
веб-интерфейс (http://127.0.0.1:8765) на кейсе тестовой корзины **TB-04**.
Каноническое конкурсное видео — `Athanor_Ouroboros_Project_Results_Demo.mp4`
(это видео его не заменяет).

## Файлы

| Файл | Назначение |
|---|---|
| `Ouroboros_E2E_Demo_2026-07-20.mp4` | видео (H.264/AAC) |
| `Ouroboros_E2E_Demo_2026-07-20.srt` | субтитры (рус., текст озвучки) |
| `Ouroboros_E2E_Demo_2026-07-20_cover.png` | обложка (стоп-кадр итоговой таблицы) |

## Параметры

- **Продолжительность:** 02:28.6
- **Сценарий:** test-basket/TB-04 (расшифровка с решениями и поручениями:
  2 решения, 3 поручения, 2 идеи-дистрактора)
- **Разрешение/частота:** 1920×1080, 30 fps
- **Кодеки:** H.264 (libx264, CRF 19) + AAC 160k (моно 44.1 кГц)
- **Размер:** 31 327 143 байт (~29.9 МБ)
- **Запись:** Playwright (headless Chromium, record_video 1920×1080) поверх
  реального веб-интерфейса Ouroboros v6.64.3
- **Озвучка:** синтез edge-tts (ru-RU-DmitryNeural), текст в `.srt`

## Что показано (таймкоды)

| Время | Сегмент |
|---|---|
| 0:00–0:06 | титул |
| 0:06–0:18 | входные данные TB-04: фрагменты calendar.json / tracker.json / mail.json / transcript.txt |
| 0:18–0:48 | запуск: задача агенту в UI (проект athanor-release-sync), статус running (1×) |
| 0:48–1:13 | работа агента: контекст → сводка → решения/поручения → черновики → HITL (**ожидание ускорено ×24**, помечено в кадре) |
| 1:13–1:45 | post-task review и завершение задачи (1×) |
| 1:45–2:18 | результат в чате: команда reject, таблица статусов D01/D02/D03, краткий итог (1×; финальный кадр удержан стоп-кадром) |
| 2:18–2:29 | финальная карточка с итогами |

## Прогон (факты)

- **Задача Ouroboros:** `dde2f6e6`, project `athanor-release-sync`,
  status **completed**, outcome tier **solved**. Поставлена через
  `POST /api/tasks` 2026-07-20 20:29 (локальное время), завершена ~20:46
  (~17 мин реального времени; роутер jetnight в этот час работал с ретраями —
  видео честно помечает ускорение ожидания).
- **Сценарий внутри задачи:** обзор входных данных TB-04 → `run --case
  test-basket/TB-04 --engine rule` → `approve --draft TB-04-D01 --execute` →
  `reject --draft TB-04-D02 --reason duplicate` → финальные статусы из outbox.
- **Подтверждённый результат** (артефакты в workspace-клоне Ouroboros
  `C:\Users\senik\Ouroboros\repo\e2e_demo\results\scratch\ouroboros_e2e_demo_20260720\`):
  run.json — 2 решения, 3 поручения; outbox: **D01 executed · D02 rejected
  (duplicate) · D03 awaiting_approval**; memory_after сохранена.
- **Исходные записи** (три записи одного live-прогона, монтаж без
  постановочных вставок): `out/video/e2e_demo_20260720/` в рабочей копии
  внешнего репо (raw webm ~90 МБ — локально, вне git; скрипты записи — в git)
  (page@*.webm + ключевые скриншоты + e2e_task_result.json), скрипты
  `record_e2e_demo_20260720.py`, `record_e2e_finale_20260720.py`,
  `record_e2e_results_20260720.py`, сборка `build_e2e_demo_video.py`.

## Честные пометки

- Средняя часть (ожидание работы агента) ускорена ×24 — помечено в кадре и
  в озвучке; остальные сегменты в реальном темпе (1×).
- Титульная, входная и финальная карточки — статичные вставки (PIL);
  всё остальное — запись живого UI.
- Все данные — обезличенная синтетика проекта «Альфа» (test-basket/TB-04).
- В записанном прогоне чат агента упоминает «блокеров: 1 (OPS-77)», а run.json
  rule-движка дал 0 — на момент записи навык не извлекал блокеры из расшифровки.
  Расхождение устранено в тот же день (CHANGELOG 1.10.0, канонический прогон
  `eval_20260720T_blockers`): `extract_blockers(..., decisions)` теперь даёт на
  TB-04 структурированный блокер OPS-77 (owner=SRE), эталон обновлён.
