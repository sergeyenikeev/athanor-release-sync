# Зависимостей нет: прототип работает на стандартной библиотеке Python 3.10+.
# LLM вызывается через urllib (OpenAI-совместимый API), MCP-заглушки — http.server.
# Файл оставлен, чтобы `pip install -r requirements.txt` был валидным шагом CI.
